import { Container, Box } from "@mui/system";

const Header = () => {
    return(
        <Box>
        <Container>
            <div>
                Kiara The Web Dev
            </div>
            My header is v plain and I want it to smooth scroll w the whole page
        </Container>
        </Box>
    );
};

export default Header